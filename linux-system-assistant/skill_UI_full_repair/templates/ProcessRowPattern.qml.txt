// Pattern ProcessRow

Rectangle {
    id: row
    height: Metrics.processRowHeight
    width: parent.width
    radius: Metrics.radiusButton
    color: hovered ? Theme.glassCardHover : Theme.glassCard
    clip: true

    property bool hovered: false
    property string processName: ""
    property string iconSource: ""
    property string fallbackLetter: "?"

    Row {
        anchors.fill: parent
        anchors.leftMargin: 12
        anchors.rightMargin: 12
        spacing: 10

        // Icon area 30x30
        // Name text uses elide
        // CPU/RAM fixed columns
        // Kill button fixed width
    }
}
